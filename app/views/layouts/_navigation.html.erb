<nav class="navbar-expand-lg navbar-light bg-light custom-navbar">
  <div class="container">
    <div class="custom-nav-link">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Hi, #{current_user.name}", root_path ,class: "btn btn-primary rounded text-white font-weight-bold px-2" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-danger ml-2'%>
        </li>

      <% else %>
        <a class="navbar-brand" href="#">Voting Machine</a>
      <% end %>
    </div>
  </div>
</nav>
